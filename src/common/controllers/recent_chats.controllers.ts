import type { SocketParams } from "../../types.js";

export async function joinRecentChatsRoom({
	io: _io,
	socket,
	user,
}: SocketParams) {}

export async function leaveRecentChatsRoom({
	io: _io,
	socket,
	user,
}: SocketParams) {}
